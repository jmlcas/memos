version: "3.3"

services:

  memos:
    image: ghcr.io/usememos/memos:latest
    container_name: memos
    init: true
    volumes:
      - ./memos/:/var/opt/memos
    ports:
      - "8200:5230"
    restart: unless-stopped
    